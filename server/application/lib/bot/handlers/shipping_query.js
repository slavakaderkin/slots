/* eslint-disable camelcase */
async (params) => {
  console.info('lib/bot/handlers/shipping_query');
  console.debug({ params });

  const { invoice_payload, id, shipping_address } = params;


  try {
    
  } catch (e) {
    console.error(e);
  }
};
